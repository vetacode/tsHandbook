//install npm install -g typescript
// Greets the world.
console.log('Hello world!');

function greet(person, date) {
  console.log(`Hello ${person}, today is ${date}!`);
}

greet('Brendan'); // In TypeScript, errors are detected at compile time, so we can avoid runtime errors before running the code.
